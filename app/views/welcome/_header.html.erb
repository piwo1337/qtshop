<nav class="navbar navbar-expand-lg navbar-light">
  <%= link_to (fa_icon "gitlab", text: "GEM Shop"), root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item hvr-float">
        <%= active_link_to'Home', root_path, class: "nav-link", active: :exclusive %>
      </li>
      <li class="nav-item hvr-float">
        <%= active_link_to "Gem's", gems_path, class: "nav-link"  %>
      </li>
      <li class="nav-item hvr-float">
        <%= active_link_to "About", about_path, class: "nav-link"  %>
      </li>
      <li class="nav-item hvr-float">
        <%= active_link_to "Contact", contact_path, class: "nav-link"  %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <% if admin_signed_in? %>
          Admin Account
        <% else %>
          Account
        <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <% if admin_signed_in? %>
          <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link dropdown-item'  %>
        <% else %>
          <%= link_to "Login", new_admin_session_path, :class => 'navbar-link dropdown-item'  %>
        <% end %>
      </div>
      </li>
    </ul>
  </div>
</nav>
